This is an example Markdown file that can be included in templates.

You can include this file in your template using the `include` function.

## Example Usage

In your template file (`doc.md.tmpl`), use:

```
{{ include "example.md" }}
```

This will inject the contents of this Markdown file at that position.
