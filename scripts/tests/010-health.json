{
  "docURL": "http://localhost:6000",
  "serverURL": "http://localhost:6000",
  "prefix": "",
  "tests": [
    {
      "name": "Health Check",
      "cmd": "GET",
      "endpoint": "/health",
      "details": "Returns server health status. No authentication required."
    },
    {
      "name": "Root Endpoint",
      "cmd": "GET",
      "endpoint": "/",
      "details": "`/` is an alias for `/health`."
    }
  ]
}
